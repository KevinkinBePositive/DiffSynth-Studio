edge detection
    prompt="A static pixel-aligned visual transformation of the input image into an edge map. The camera is completely fixed, producing a still image with no motion. Every object, boundary, shape, and layout element stays at the exact same image coordinates throughout the video. No object motion, no camera movement, no parallax, no zoom, no pan, no tilt. Across time, the original image gradually fades away while all visible edges become more salient and transform into clean black outlines. The final frame shows only the preserved object boundaries and structural edges as black lines on a pure white background. Preserve the exact geometry, positions, proportions, and layout of the input image; only the visual representation changes from the original appearance to an edge-map style.",
    
    negative_prompt="motion, moving objects, walking, drifting, displacement, camera movement, camera pan, camera tilt, camera zoom, dolly, handheld camera, shake, parallax, viewpoint change, perspective change, object translation, object rotation, scene movement, background movement.",

seg
    prompt="A static pixel-aligned visual transformation of the input photograph into an instance segmentation map. The video starts from the provided unaltered photograph. The camera is completely fixed, producing a still scene with no motion. Every object, entity, boundary, shape, and layout element stays at the exact same image coordinates throughout the video. No object motion, no camera movement, no parallax, no zoom, no pan, no tilt, no rotation, no viewpoint change. Across time, only the visual representation changes. First, the background gradually fades to pure white while the foreground objects remain in their original positions. Then, each distinct visible entity is gradually covered by a different flat solid color, one entity at a time, perfectly preserving its original silhouette, boundary, shape, size, and position. Each entity receives a unique color. Colors are flat, opaque, and uniform, with no texture, shading, gradients, shadows, or photorealistic detail. The final frame shows a clean instance segmentation result: a pure white background and all distinct entities covered with different flat colors. Preserve the exact geometry, positions, proportions, object boundaries, and layout of the input photograph. Same scene, same coordinates, same geometry; only the representation changes into an instance segmentation visualization. No glitches, artifacts, flicker, deformation, drifting, or newly generated objects.",
    
    negative_prompt="motion, moving objects, walking, drifting, displacement, camera movement, camera pan, camera tilt, camera zoom, dolly, handheld camera, shake, parallax, viewpoint change, perspective change, object translation, object rotation, scene movement, background movement.",
    